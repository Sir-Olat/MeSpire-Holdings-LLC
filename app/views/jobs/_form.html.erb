<%= form_with(model: job, local: true) do |form| %>
    <% if job.errors.any? %>
        <div id="error_explanation">
          <h5><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h5>

          <ul>
            <% job.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= form.label :title %>
      <%= form.text_field :title, id: :job_title %>
    </div>

    <div class="field">
      <%= form.label :description %>
      <%= form.text_area :description, id: :job_description %>
    </div>

    <div class="field">
      <%= form.label :location %>
      <%= form.text_field :location, id: :job_location %>
    </div>

    <div class="field">
      <%= form.label :job_type %>
      <%= form.text_field :job_type, id: :job_type %>
    </div>

    <div class="field">
      <%= form.label :salary_range %>
      <%= form.text_field :salary_range, id: :job_salary_range %>
    </div>

    <br>

    <div class="actions">
      <%= form.submit 'Submit', class: 'waves-effect waves-light btn', style: 'margin: 10px; !important' %>
    </div>

<% end %>
